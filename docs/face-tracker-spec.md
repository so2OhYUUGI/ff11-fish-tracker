/**
 * ============================================================================
 * [FilePath] docs/face-tracker-spec.md (Conceptual / Specification)
 * [Role]     フェイス取得チェッカーの機能仕様およびデータ構造の定義
 * 
 * [概要]
 * - FF11のフェイス取得進捗管理および「集めたいフェイス（最大3つの目標リスト）」、
 *   および「マイフェイス編成（パーティ構成）」の管理・共有機能を提供する。
 * - サーバーレスでのユーザー間自発的共有（URLクエリパラメータのエンコード方式）を採用。
 * 
 * [データ構造仕様]
 * - フェイスマスターデータ (faceId, name, enName, role, category, obtainMethod)
 * - ユーザー進捗 (checkedFaceIds: number[], targetLists: { id: string, name: string, faceIds: number[] }[], myParties: { id: string, name: string, memberFaceIds: number[] }[])
 * 
 * [編集・改修時の注意事項]
 * 1. 【UI/UXの踏襲】釣魚チェッカーの3段構成カード、高密度横並びリストの設計思想をそのまま適用すること。
 * 2. 【サーバーレス共有】データの共有はURLエンコード方式を維持し、バックエンドDBに依存しない設計とすること。
 * ============================================================================
 */

# フェイス取得チェッカー 仕様書

## 1. 概要
- **目的**: FF11のフェイス取得進捗管理、ログインキャンペーン等を見据えた「今から集めたいフェイス（最大3つの目標リスト）」、および「マイフェイス編成」の作成とユーザー間共有。
- **対象ユーザー**: 複数キャラクターを管理し、パーティ構成や取得状況を整理したいプレイヤー。

---

## 2. データ構造仕様 (`src/types/facetracker.ts` 等)

### **フェイスマスターデータ**
- `faceId`: 数値ID
- `name`: 日本語名
- `enName`: 英語名
- `role`: ロール分類（盾 / アタッカー / 回復 / 支援 等）
- `category`: 入手カテゴリ（ミッション・クエスト / ログインキャンペーン / 星唄報酬 等）
- `obtainMethod`: 取得方法の詳細説明文

### **ユーザー進捗 (`CharacterProgress` の拡張)**
- `checkedFaceIds`: 所持済みフェイスID（`number[]`）
- `targetLists`: 最大3つの目標リスト（`{ id: string, name: string, faceIds: number[] }[]`）
- `myParties`: マイフェイス編成（`{ id: string, name: string, memberFaceIds: number[] }[]`）

---

## 3. 主要機能仕様

### **① 一覧と取得チェック・目標チェック**
- 所持済みチェックと、最大3つの目標リストへの振り分け機能。
- 釣魚チェッカーの共通カード・リスト UI/UX 標準の踏襲。

### **② フィルタリングと詳細表示**
- ロール別、入手カテゴリ別、達成状態での絞り込み。
- 詳細ビューによる具体的な入手手順の確認。

### **③ ユーザー間自発的シェア機能**
- 目標リストやマイフェイス編成をURLセーフな文字列にエンコードし、サーバーを介さずワンクリックでインポート・共有できる機能。